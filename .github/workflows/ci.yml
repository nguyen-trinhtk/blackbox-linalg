name: Build and Test

on: [push, pull_request]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t wiedemann-analyzer .

      - name: Run main test
        run: docker run wiedemann-analyzer

      # Wait until finalizing tests
      # - name: Run matvec test
      #   run: docker run wiedemann-analyzer ./matvec

      # - name: Run bm test
      #   run: docker run wiedemann-analyzer ./bm

      # - name: Run wiedemann test
      #   run: docker run wiedemann-analyzer ./wiedemann

        # probably add failure noti